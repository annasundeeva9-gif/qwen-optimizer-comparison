defaults:
  - mode: mock
  - model: tiny
  - data: openwebtext_100k
  - optimizer: adamw
  - training: default
  - evaluation: lm_eval
  - tracking: mlflow
  - experiment: mock_adamw
  - _self_

project:
  name: optimizer-comparison
  output_dir: outputs

artifacts:
  root_dir: outputs/runs
  config_filename: config.yaml
  result_filename: result.json
  hf_hub:
    enabled: false
    repo_id: null
    token_env_var: HF_TOKEN
    private: true
