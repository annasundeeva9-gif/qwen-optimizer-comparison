name: smoke
description: "Small end-to-end run for validating the training loop."
use_mock_trainer: false
smoke_samples_limit: 1000
limit_eval_samples: 32

data:
  split:
    dir: outputs/datasets/split_raw/openwebtext_100k_smoke
  tokenization:
    dir: outputs/datasets/tokenized/openwebtext_100k_smoke
  final:
    dir: outputs/datasets/final/openwebtext_100k_smoke
